package DSA.SortingAlgorithms;

public class InsertionSort {
    public static void main(String[] args) {
        //create outer loop
        //check if a[0] > a[1] (inner loop 1)
        //check if a[1] < a[0] (inner loop 2)
    }
}
